spring:
  application:
    name: spring-init-project
  profiles:
    active: local

  # ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/interview_practice?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
      password:
      ttl: 3600

server:
  port: 8150
  servlet:
    context-path: /api



# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.guochang.interviewpracticebackend.controller

# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

# mybatis-plus
mybatis-plus:
  configuration:
    # MyBatis ??
    map-underscore-to-camel-case: false
    # ??????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # ????????????
      logic-delete-value: 1 # ?????????? 1?
      logic-not-delete-value: 0 # ?????????? 0?